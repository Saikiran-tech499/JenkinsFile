pipeline {
    agent any

    stages {
        stage('SampleBuildJob') {
            steps {
                echo 'SampleBuildJob'
            }
        }

        stage('SampleDeployJob') {
            steps {
                echo 'SampleDeployJob'
            }
        }

        stage('SampleTestJob') {
            steps {
                echo 'SampleTestJob'
            }
        }

        stage('SampleReleaseJob') {
            steps {
                echo 'SampleReleaseJob'
            }
        }
    }

    post {
        always {
            echo 'Pipeline execution completed.'
        }

        success {
            echo 'Pipeline execution successful.'
        }

        failure {
            echo 'Pipeline execution failed.'
        }
    }
}

